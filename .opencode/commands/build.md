---
description: Implement one accepted, bounded vertical slice
agent: build
---

Implement this accepted task:

$ARGUMENTS

Follow `AGENTS.md`. If the task crosses a trust boundary or requires an unresolved architecture decision, do not guess: report the blocker and point to the missing decision in `docs/PLAN.md`. Otherwise make the smallest coherent change, add risk-based tests, inspect the diff, and run verification.
